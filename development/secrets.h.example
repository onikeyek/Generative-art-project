// secrets.h.example — safe to commit. Shows the required shape without
// real values. Copy this to secrets.h (which is gitignored) and fill in
// your own WiFi credentials, backend URL, and API key.

const char* ssid        = "your-wifi-name";
const char* password    = "your-wifi-password";
const char* ingest_url  = "https://yourusername.pythonanywhere.com/api/ingest";
const char* api_key     = "must-match-backend-INGEST_API_KEY";
